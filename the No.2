#include<stdio.h>
#include<windows.h>
int main()
{
	char a[20] = "welcome to bit!!!!!";
	char b[] = "###################";//注意字符串的结尾有一个结束符\0占一个位
	printf("%s\n", b);
	for (int i = 0; i < 20; i++)
	{
		b[i] = a[i];
		b[18 - i] = a[18 - i];//所以是20-2，而不是减一
		printf("%s\n", b);
		Sleep(1000);
		if (i == 9)
		{
			break;
		}
	}
	return 0;
}
